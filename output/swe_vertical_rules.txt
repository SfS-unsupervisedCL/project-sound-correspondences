IF deu_itself_sound_type in {N/A, dot, word boundary, consonant} AND deu_itself_place is N/A AND swe_prevNonDot_manner is N/A THEN N/A
IF deu_itself_sound_type in {N/A, dot, word boundary, consonant} AND deu_itself_place is N/A AND swe_prev_manner in {N/A, plosive, tap, trill, affricate} AND swe_prevNonDot_manner is applicable THEN open
IF deu_itself_sound_type in {N/A, dot, word boundary, consonant} AND deu_itself_place is N/A AND swe_prev_manner in {fricative, lateral fricative, lateral approximant, approximant, nasal} AND swe_prevNonDot_manner is applicable THEN N/A
IF deu_itself_sound_type is vowel AND deu_itself_horizontal in {N/A, back, near-back, central} AND deu_prevOrSelfVowel_vertical in {near-open, open-mid, mid} AND swe_prevVowel_horizontal is N/A THEN open-mid
IF deu_itself_sound_type is vowel AND deu_itself_horizontal in {N/A, back, near-back, central} AND deu_prevOrSelfNonDot_horizontal in {N/A, back} AND deu_prevOrSelfVowel_vertical in {close-mid, near-close, close} AND swe_prevVowel_horizontal is N/A THEN close
IF deu_itself_sound_type is vowel AND deu_itself_horizontal in {N/A, back, near-back, central} AND deu_prevOrSelfNonDot_horizontal in {near-back, central, near-front, front} AND deu_prevOrSelfVowel_vertical in {close-mid, near-close, close} AND swe_prevVowel_horizontal is N/A THEN close-mid
IF deu_itself_sound_type is vowel AND deu_itself_horizontal is front AND deu_prevOrSelfVowel_vertical in {N/A, open, near-open, open-mid} AND swe_prevNonDot_voice is applicable AND swe_prevVowel_horizontal is applicable THEN open-mid
IF deu_itself_sound_type is vowel AND deu_prevOrSelfVowel_length in {N/A, normal, half-long} AND deu_prevOrSelfVowel_vertical in {close-mid, near-close, close} AND swe_prevNonDot_voice is applicable AND swe_prevVowel_horizontal is applicable THEN near-close
IF deu_itself_sound_type is vowel AND deu_prevOrSelfVowel_length is long AND deu_prevOrSelfVowel_vertical in {close-mid, near-close, close} AND swe_prevNonDot_voice is applicable AND swe_prevVowel_horizontal is applicable THEN open-mid
IF deu_itself_sound_type is vowel AND deu_itself_horizontal in {near-front, front} AND deu_prevOrSelfNonDot_horizontal in {N/A, back, near-back, central, near-front} AND deu_prevOrSelfVowel_vertical in {near-open, open-mid, mid, close-mid, near-close, close} AND swe_prevVowel_horizontal is N/A THEN near-close
IF deu_itself_sound_type is vowel AND deu_itself_horizontal in {near-front, front} AND deu_prevOrSelfNonDot_horizontal is front AND deu_prevOrSelfVowel_vertical in {close-mid, near-close, close} AND swe_prevVowel_horizontal is N/A THEN close-mid
IF deu_itself_sound_type is vowel AND deu_itself_horizontal in {N/A, back, near-back, central, near-front} AND deu_prevOrSelfVowel_vertical in {N/A, open, near-open, open-mid} AND swe_prevNonDot_voice is applicable AND swe_prevVowel_horizontal is applicable THEN mid
IF deu_itself_sound_type is vowel AND deu_itself_horizontal in {near-front, front} AND deu_prevOrSelfNonDot_horizontal is front AND deu_prevOrSelfVowel_vertical in {near-open, open-mid, mid} AND swe_prevVowel_horizontal is N/A THEN open-mid
IF deu_itself_sound_type is vowel AND swe_prevNonDot_voice is N/A AND swe_prevVowel_horizontal is applicable THEN N/A
IF deu_itself_sound_type is vowel AND deu_prevOrSelfVowel_vertical in {N/A, open} AND swe_prevVowel_horizontal is N/A THEN open
IF deu_itself_sound_type is vowel AND deu_prevOrSelfVowel_vertical is mid AND swe_prevNonDot_voice is applicable AND swe_prevVowel_horizontal is applicable THEN open
IF deu_itself_sound_type in {N/A, dot, word boundary, consonant} AND deu_itself_place is applicable THEN N/A
