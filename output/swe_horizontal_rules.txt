IF deu_itself_horizontal is applicable AND deu_prevOrSelfNonDot_horizontal is near-back AND swe_prevNonDot_rounding is N/A THEN central
IF deu_itself_horizontal in {back, near-back, central} AND deu_prevOrSelfNonDot_vertical in {N/A, open, near-open, open-mid} AND deu_prevOrSelfNonDot_horizontal in {central, near-front, front} AND swe_prevNonDot_rounding is N/A THEN central
IF deu_itself_horizontal in {back, near-back, central} AND deu_prev_manner in {N/A, plosive, tap, trill, affricate} AND deu_prevOrSelfNonDot_vertical in {mid, close-mid, near-close, close} AND deu_prevOrSelfNonDot_horizontal in {central, near-front, front} AND swe_prevNonDot_rounding is N/A AND swe_prevVowel_horizontal in {N/A, back, near-back} THEN central
IF deu_itself_horizontal in {back, near-back, central} AND deu_prev_manner in {fricative, lateral fricative, lateral approximant, approximant, nasal} AND deu_prevOrSelfNonDot_vertical in {mid, close-mid, near-close, close} AND deu_prevOrSelfNonDot_horizontal in {central, near-front, front} AND swe_prevNonDot_rounding is N/A AND swe_prevVowel_horizontal in {N/A, back, near-back} THEN front
IF deu_itself_horizontal is front AND deu_prevOrSelfNonDot_vertical in {N/A, open, near-open} AND deu_prevOrSelfNonDot_horizontal in {central, near-front, front} AND deu_prevOrSelfVowel_length is long AND swe_prevNonDot_rounding is N/A THEN back
IF deu_itself_horizontal is applicable AND deu_prevOrSelfNonDot_horizontal in {N/A, back} AND swe_prevNonDot_rounding is N/A THEN back
IF deu_itself_horizontal is near-front AND deu_prevOrSelfNonDot_horizontal in {central, near-front, front} AND swe_prevNonDot_rounding is N/A THEN near-front
IF deu_itself_horizontal is front AND deu_prevOrSelfNonDot_vertical in {N/A, open, near-open} AND deu_prevOrSelfNonDot_horizontal in {central, near-front, front} AND deu_prevOrSelfVowel_length in {N/A, normal, half-long} AND swe_prevNonDot_rounding is N/A THEN front
IF deu_itself_horizontal is applicable AND swe_prevNonDot_rounding is applicable THEN N/A
IF deu_itself_horizontal in {back, near-back, central} AND deu_prevOrSelfNonDot_vertical in {mid, close-mid, near-close, close} AND deu_prevOrSelfNonDot_horizontal in {central, near-front, front} AND swe_prevNonDot_rounding is N/A AND swe_prevVowel_horizontal in {central, near-front, front} THEN front
IF deu_itself_horizontal is front AND deu_prevOrSelfNonDot_vertical in {open-mid, mid, close-mid, near-close, close} AND deu_prevOrSelfNonDot_horizontal in {central, near-front, front} AND swe_prevNonDot_rounding is N/A THEN front
IF deu_itself_horizontal is N/A THEN N/A
